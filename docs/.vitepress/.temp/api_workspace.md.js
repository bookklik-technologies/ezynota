import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Workspace API","description":"","frontmatter":{},"headers":[],"relativePath":"api/workspace.md","filePath":"api/workspace.md","lastUpdated":null}');
const _sfc_main = { name: "api/workspace.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="workspace-api" tabindex="-1">Workspace API <a class="header-anchor" href="#workspace-api" aria-label="Permalink to &quot;Workspace API&quot;">​</a></h1><p>In workspace and document modes, <code>editor.workspace</code> exposes a <code>WorkspaceController</code> — the session layer that swaps documents, keeps per-note undo histories, and drives autosave.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> editor.ready;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> ws</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> editor.workspace;</span></span></code></pre></div><h2 id="sessions" tabindex="-1">Sessions <a class="header-anchor" href="#sessions" aria-label="Permalink to &quot;Sessions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Member</th><th>Description</th></tr></thead><tbody><tr><td><code>state</code></td><td>The underlying <code>WorkspaceState</code> (see below).</td></tr><tr><td><code>start()</code></td><td>Load the workspace and open the first note (or the initial data as a new note).</td></tr><tr><td><code>openNoteById(id)</code></td><td>Open a note — swaps the document <strong>and</strong> its per-note undo history.</td></tr><tr><td><code>getActiveNoteId()</code></td><td>Current note id, or <code>null</code>.</td></tr><tr><td><code>saveActiveNote()</code></td><td>Force-save the active note now.</td></tr><tr><td><code>retryLoad()</code> / <code>retrySave()</code></td><td>Re-attempt after load/save failures.</td></tr><tr><td><code>destroy()</code></td><td>Flush pending saves and tear down.</td></tr></tbody></table><p>Per-note history: undo stacks are kept per note in <code>histories: Map&lt;string, HistoryState&gt;</code> — switching notes preserves each note&#39;s undo position.</p><h2 id="content-access" tabindex="-1">Content access <a class="header-anchor" href="#content-access" aria-label="Permalink to &quot;Content access&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Member</th><th>Description</th></tr></thead><tbody><tr><td><code>portableSnapshot()</code></td><td>Active note&#39;s document with <code>asset:&lt;id&gt;</code> sources resolved to data URLs.</td></tr><tr><td><code>activeNoteHtml()</code></td><td>Active note rendered to sanitized HTML.</td></tr><tr><td><code>exportActiveNote(format)</code></td><td>Export as <code>&quot;json&quot; | &quot;md&quot; | &quot;html&quot; | &quot;txt&quot;</code> (triggers download).</td></tr><tr><td><code>importFiles(files)</code></td><td>Import json / md / html / txt / workspace backups.</td></tr><tr><td><code>importLegacyDraft(data)</code></td><td>Recover data from older draft formats.</td></tr><tr><td><code>wordCount()</code></td><td>Word count of the active note.</td></tr><tr><td><code>findInDocument(query, replaceWith?, all?)</code></td><td>Find &amp; replace within the active note.</td></tr></tbody></table><h2 id="search-links" tabindex="-1">Search &amp; links <a class="header-anchor" href="#search-links" aria-label="Permalink to &quot;Search &amp; links&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Member</th><th>Description</th></tr></thead><tbody><tr><td><code>search(query)</code></td><td>Full-text search — <code>SearchHit[]</code> across notes.</td></tr><tr><td><code>backlinks(noteId)</code></td><td>Notes whose content links to this one.</td></tr><tr><td><code>listNotes(includeTrashed?)</code></td><td><code>NoteRecord[]</code>.</td></tr><tr><td><code>listFolders(includeTrashed?)</code></td><td><code>FolderRecord[]</code>.</td></tr></tbody></table><h2 id="fullscreen-theme" tabindex="-1">Fullscreen &amp; theme <a class="header-anchor" href="#fullscreen-theme" aria-label="Permalink to &quot;Fullscreen &amp; theme&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Member</th><th>Description</th></tr></thead><tbody><tr><td><code>toggleFullscreen(force?)</code></td><td><code>Promise&lt;void&gt;</code> — enter or exit browser fullscreen. Call from a user action such as a click. Failures show a notice.</td></tr><tr><td><code>isFullscreen()</code></td><td>Current state.</td></tr><tr><td><code>setTheme(t)</code> / <code>getTheme()</code></td><td><code>&quot;light&quot; | &quot;dark&quot; | &quot;system&quot;</code>.</td></tr><tr><td><code>resolvedTheme()</code></td><td>Effective <code>&quot;light&quot; | &quot;dark&quot;</code>.</td></tr></tbody></table><p>The fullscreen button uses the browser&#39;s Fullscreen API. Escape exits fullscreen, and <code>fullscreen:changed</code> reports the actual browser state. Embedded editors need fullscreen allowed by their containing iframe.</p><h2 id="backups" tabindex="-1">Backups <a class="header-anchor" href="#backups" aria-label="Permalink to &quot;Backups&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Member</th><th>Description</th></tr></thead><tbody><tr><td><code>createBackup()</code></td><td><code>Promise&lt;WorkspaceBackup&gt;</code> — notes + folders + assets.</td></tr><tr><td><code>restoreBackup(backup)</code></td><td>Restore under a <strong>new</strong> workspace id by default; returns <code>{ workspaceId }</code>.</td></tr><tr><td><code>restoreBackupFile(file)</code></td><td>Restore from a downloaded backup file.</td></tr><tr><td><code>downloadStoredData()</code></td><td>Download the raw stored envelope.</td></tr></tbody></table><h2 id="workspacestate" tabindex="-1">WorkspaceState <a class="header-anchor" href="#workspacestate" aria-label="Permalink to &quot;WorkspaceState&quot;">​</a></h2><p><code>ws.state</code> is the state machine under the controller:</p><ul><li><strong>Read:</strong> <code>getEnvelope()</code>, <code>getRevision()</code>, <code>listNotes()</code>, <code>listFolders()</code>, <code>listTrashedNotes()</code>, <code>getNote(id)</code>, <code>getFolder(id)</code>, <code>folderPath(id)</code>, <code>noteLinks(id)</code>, <code>backlinks(id)</code></li><li><strong>Notes:</strong> <code>createNote(title, folderId?, document?)</code>, <code>renameNote</code>, <code>moveNote</code>, <code>duplicateNote</code>, <code>updateNoteDocument</code>, <code>trashNote</code>, <code>restoreNote</code>, <code>deleteNoteForever</code></li><li><strong>Folders:</strong> <code>createFolder</code>, <code>renameFolder</code>, <code>moveFolder</code> (cycle-proof), <code>trashFolder</code>, <code>restoreFolder</code>, <code>deleteFolderForever</code>, <code>emptyTrash</code></li><li><strong>Persistence:</strong> <code>markDirty()</code> (500 ms debounce), <code>flushSave()</code>, <code>flush()</code>, <code>getUnsavedSnapshot()</code>, <code>createBackup()</code></li><li><strong>Assets:</strong> <code>saveAsset(asset)</code>, <code>loadAsset(id)</code>, <code>assetObjectUrl(id)</code></li><li><strong>Lifecycle:</strong> <code>load()</code>, <code>isLoaded()</code>, <code>getLoadError()</code>, <code>retryLoad()</code>, <code>destroy()</code></li></ul><h2 id="autosave-conflict-safety" tabindex="-1">Autosave &amp; conflict safety <a class="header-anchor" href="#autosave-conflict-safety" aria-label="Permalink to &quot;Autosave &amp; conflict safety&quot;">​</a></h2><ul><li>Changes call <code>markDirty()</code>; a debounced save (500 ms) follows.</li><li>Commits carry a <code>storageRevision</code>; if another tab saved first, the editor surfaces a recovery state instead of overwriting (<code>workspace:changed</code> with kind <code>&quot;remoteChange&quot;</code> / <code>&quot;loadFailed&quot;</code>).</li><li><code>SaveStatus</code> drives the visible save indicator: pending / saved / error.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("api/workspace.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const workspace = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  workspace as default
};
