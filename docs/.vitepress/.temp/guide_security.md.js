import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Security","description":"","frontmatter":{},"headers":[],"relativePath":"guide/security.md","filePath":"guide/security.md","lastUpdated":null}');
const _sfc_main = { name: "guide/security.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to &quot;Security&quot;">​</a></h1><p>Ezynota is designed to render untrusted content safely. If your users paste from Word, import random HTML files, or open documents from the internet, the editor treats all of it as hostile input.</p><h2 id="the-sanitization-pipeline" tabindex="-1">The sanitization pipeline <a class="header-anchor" href="#the-sanitization-pipeline" aria-label="Permalink to &quot;The sanitization pipeline&quot;">​</a></h2><p>Everything that enters the document passes through the same validators:</p><ol><li><strong><code>normalizeDocument</code> / <code>normalizeBlock</code></strong> — strict structural validation on every write. Unknown shapes throw <code>EZ_INVALID_DOCUMENT</code> / <code>EZ_INVALID_BLOCK</code> rather than being rendered.</li><li><strong>Depth cap</strong> — JSON deeper than <code>MAX_JSON_DEPTH</code> (200) is rejected, defeating deep-nesting bombs.</li><li><strong>Prototype-pollution stripping</strong> — keys like <code>__proto__</code>, <code>constructor</code>, and <code>prototype</code> are removed from all incoming JSON.</li><li><strong><code>salvageDocument</code></strong> — for untrusted payloads that must not fail: invalid blocks become read-only <code>unknown</code> placeholders and duplicate ids are renamed (<code>&lt;id&gt;_2</code>, <code>&lt;id&gt;_3</code>, …) instead of crashing.</li></ol><h2 id="url-safety" tabindex="-1">URL safety <a class="header-anchor" href="#url-safety" aria-label="Permalink to &quot;URL safety&quot;">​</a></h2><p>A strict allowlist guards every URL that can reach the DOM:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { isSafeUrl, isSafeImageUrl, sanitizeLinkTarget } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;@bookklik/ezynota&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">isSafeUrl</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;https://example.com&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// true</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">isSafeUrl</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;javascript:alert(1)&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// false</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">isSafeUrl</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;data:text/html,…&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);      </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// false</span></span></code></pre></div><ul><li><code>javascript:</code>, <code>vbscript:</code>, and other executable schemes are stripped.</li><li><code>data:</code> URLs are only allowed for <strong>images</strong> (and only real image MIME types).</li><li>Protocol-relative URLs (<code>//evil.com</code>) are rejected.</li><li>Link targets are normalized by <code>sanitizeLinkTarget</code> before rendering.</li><li>Rendered links always get <code>rel=&quot;noopener noreferrer&quot;</code>.</li></ul><h2 id="paste-import-sanitization" tabindex="-1">Paste &amp; import sanitization <a class="header-anchor" href="#paste-import-sanitization" aria-label="Permalink to &quot;Paste &amp; import sanitization&quot;">​</a></h2><ul><li>Pasted/dropped HTML goes through <code>htmlToBlocks</code>, which builds a sanitized tree first: scripts, styles, event handler attributes, and unsafe URLs never survive.</li><li>Clipboard JSON (<code>application/x-ezynota+json</code>) is re-validated as a document — it is never trusted as-is.</li><li>File imports are size-capped (<strong>10 MB</strong>) to prevent decompression bombs and memory exhaustion.</li><li>Image sources are validated with <code>isSafeImageUrl</code> at render time.</li></ul><h2 id="dom-construction" tabindex="-1">DOM construction <a class="header-anchor" href="#dom-construction" aria-label="Permalink to &quot;DOM construction&quot;">​</a></h2><p>The renderer never assigns <code>innerHTML</code> from document data:</p><ul><li>All DOM is built with <code>createElement</code> / <code>textContent</code> / explicit attribute setting.</li><li>Text is always inserted as text nodes — marks are elements built programmatically.</li><li>Custom marks render as <code>span[data-ez-mark]</code> rather than raw tag names from data.</li></ul><h2 id="print-export" tabindex="-1">Print &amp; export <a class="header-anchor" href="#print-export" aria-label="Permalink to &quot;Print &amp; export&quot;">​</a></h2><p><code>printDocument</code> and <code>blocksToHtml</code> render through the same sanitized pipeline — exported HTML contains no scripts, handlers, or unsafe URLs from the source document.</p><h2 id="no-eval-no-remote-code" tabindex="-1">No eval, no remote code <a class="header-anchor" href="#no-eval-no-remote-code" aria-label="Permalink to &quot;No eval, no remote code&quot;">​</a></h2><ul><li>No <code>eval</code>, no <code>new Function</code>, anywhere.</li><li>Declarative attribute values (<code>data-ezn-*</code>) are parsed, never evaluated.</li><li>No runtime dependencies means no transitive supply-chain code at runtime.</li></ul><h2 id="what-s-explicitly-out-of-scope" tabindex="-1">What&#39;s explicitly out of scope <a class="header-anchor" href="#what-s-explicitly-out-of-scope" aria-label="Permalink to &quot;What&#39;s explicitly out of scope&quot;">​</a></h2><p>Ezynota is a client-side editor. Accounts, server sync, real-time collaboration, and plugin marketplaces are non-goals — which keeps the trust boundary simple: <strong>the browser is the whole runtime</strong>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/security.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const security = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  security as default
};
