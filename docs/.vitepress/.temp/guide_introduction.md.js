import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Introduction","description":"","frontmatter":{},"headers":[],"relativePath":"guide/introduction.md","filePath":"guide/introduction.md","lastUpdated":null}');
const _sfc_main = { name: "guide/introduction.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h1><p>Ezynota is a <strong>free, block-style editor library with portable JSON output</strong>. It is framework-agnostic and built for structured editing experiences — CMS platforms, documentation systems, note-taking apps, comments, and knowledge bases.</p><p>It ships as a single ESM bundle, a UMD build, TypeScript types, and one CSS file — with <strong>zero runtime dependencies</strong>.</p><h2 id="design-principles" tabindex="-1">Design principles <a class="header-anchor" href="#design-principles" aria-label="Permalink to &quot;Design principles&quot;">​</a></h2><ul><li><strong>Block-first.</strong> Every paragraph, heading, list, quote, code block, or divider is a typed block in a predictable document tree.</li><li><strong>JSON-first, never opaque HTML.</strong> Inline rich text is stored as portable, versioned JSON (<code>schemaVersion</code> 1.0.0) — never as opaque HTML strings. Lossless Markdown and HTML interchange is built in.</li><li><strong>Transaction-driven.</strong> Every edit becomes a typed transaction against the document model with all-or-nothing commit and mechanical undo. The DOM is only a view of the document state.</li><li><strong>Zero runtime dependencies.</strong> A single ESM bundle, a UMD build, types, and one CSS file — drop it anywhere from bundlers to plain script tags.</li><li><strong>Accessible.</strong> Keyboard-complete operation, ARIA menus and live regions, RTL support, reduced-motion handling, and a WCAG 2.2 AA target.</li><li><strong>Secure by default.</strong> Pasted HTML is sanitized, dangerous URLs are rejected, prototype-pollution keys are stripped, and the DOM is built without <code>eval</code> or untrusted <code>innerHTML</code>.</li></ul><h2 id="what-s-in-the-box" tabindex="-1">What&#39;s in the box <a class="header-anchor" href="#what-s-in-the-box" aria-label="Permalink to &quot;What&#39;s in the box&quot;">​</a></h2><ul><li>Built-in block types: paragraph, headings (H1–H6), lists (bulleted, ordered, tasks), quote, code, delimiter, tables with rich-text cells, images, callouts, and collapsible toggle sections</li><li>Four lifecycle modes: <strong>workspace</strong>, <strong>document</strong>, <strong>embedded</strong>, and <strong>headless</strong></li><li>Declarative <code>data-ezn-editor</code> initialization with <code>ezn:ready</code>, <code>ezn:change</code>, and <code>ezn:error</code> DOM events, plus an <code>ezynota/core</code> entry without the auto-scan side effect</li><li>A browser-local workspace: notes, folders, linked notes, trash, search, autosave, and backups on IndexedDB</li><li>Document-level undo/redo, find/replace, slash menu, and Markdown typing shortcuts</li><li>Versioned JSON schema with deterministic offline migrations; unknown schema versions are preserved</li></ul><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><a href="/ezynota/guide/getting-started">Getting started</a> — installation, initialization, and first steps</li><li><a href="/ezynota/guide/configuration">Configuration</a> — every option explained</li><li><a href="/ezynota/guide/modes">Modes</a> — workspace, document, embedded, headless</li><li><a href="/ezynota/api/editor">API reference</a> — the <code>Ezynota</code> class, events, and commands</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/introduction.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const introduction = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  introduction as default
};
